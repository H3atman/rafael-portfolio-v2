# CLAUDE.md

This project keeps a single set of agent instructions, in `AGENTS.md` at the repo root:
architecture, commands, component/content/SEO conventions, testing, and the `## Agent skills`
section that points at `docs/agents/`.

They used to be described twice — here and there — in slightly different words, which drifts.
`AGENTS.md` is the source of truth; add anything new there, not here.

@AGENTS.md
